on: [push, delete]

jobs:
  to_gitlab:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: pixta-dev/repository-mirroring-action@v1
        with:
          REMOTE: 'https://github.com/EpitechPromo2028/B-DOP-200-COT-2-1-bschocolatine-brouhane.boni-gomina.git'
          GIT_USERNAME: Dinezimo
          GIT_PASSWORD: 'Dinichou1234'
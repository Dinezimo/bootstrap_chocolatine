on: [push]

jobs:
    repo-size:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
              with:
                fetch-depth: 0
            - run: echo "la taille de ton repo est $GITHUB_REPOSITORY_SIZE bytes" && du -skh --exclude=".git